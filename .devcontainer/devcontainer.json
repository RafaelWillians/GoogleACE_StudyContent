{
	"name": "Codespace Main",		
	"dockerFile": "Dockerfile",
	// "postCreateCommand": "code -r . && code --new-window --wait && bash -c 'chmod u+x .devcontainer/postCreate.sh && .devcontainer/postCreate.sh'",
	"postCreateCommand": "bash -c 'chmod u+x .devcontainer/postCreate.sh && .devcontainer/postCreate.sh'",
	
	"features": {
		"ghcr.io/devcontainers/features/github-cli:1": {}
	},	
	"customizations": {
		"vscode": {
			"settings": {
				"terminal.integrated.defaultProfile.linux": "bash"
			},
			// "tasks": {
			// 	"label": "Google Cloud Login",
			// 	"type": "shell",
			// 	"command": "chmod u+x .devcontainer/postCreate.sh && .devcontainer/postCreate.sh",
			// 	"group": {
			// 		"kind": "build",
			// 		"isDefault": true
			// 	}

			// },
			"extensions": [
			"mhutchie.git-graph",
			"phil294.git-log--graph",			
			"github.vscode-github-actions",
			"GitHub.copilot",
			"GitHub.copilot-chat",
			"bierner.github-markdown-preview"
		]
	  }
	}
  }
